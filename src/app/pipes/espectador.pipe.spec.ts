import { EspectadorPipe } from './espectador.pipe';

describe('EspectadorPipe', () => {
  it('create an instance', () => {
    const pipe = new EspectadorPipe();
    expect(pipe).toBeTruthy();
  });
});
